@using Nota.Site.Generator
@model Stasistium.Documents.IDocument;
@{
    var l = Model.Metadata.GetValue<PageLayoutMetadata>()?.Layout;
    if (String.IsNullOrWhiteSpace(l))
    {
        l = @"base.cshtml";
    }
    Layout = l;
}